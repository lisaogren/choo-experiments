import html from 'choo/html'

export default (state, emit) => {
  return html`
    <section class="section">
      <div class="container">
        <h1 class="title">Editor</h1>
        <hr>
      </div>
    </section>
  `
}
